name: plantin_test_task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  go_router: ^16.3.0
  shared_preferences: ^2.5.3
  dio: ^5.9.0
  lazy_load_scrollview: ^1.3.0
  photo_view: ^0.15.0
  image_picker: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
